{
    // ts 파일이 포함되는 폴더
    "include": ["src"],
    "compilerOptions": {

        // 컴파일된 js파일이 저장되는 dir
        "outDir": "build",

        // esModuleInterop 속성이 위의 코드 처럼 true로 설정될 경우, ES6 모듈 사양을 준수하여 CommonJS 모듈을 가져올 수 있게 됩니다.
        // 해당 옵션을 통해 위에서 예시로 들었던 코드는 아래와 같이 트랜스파일링 되며, 그 결과 정상적으로 import 하는 것이 가능해집니다.
        "esModuleInterop":true,

        // js 버전 allow Fn은 ES5 부터 지원. 대부분 ES6을 사용하는 것이 가장 안전함
        "target": "ES6",

        // js 버전과 자바스크립트가 실행된 환경 // DOM을 안 붙여주면 백엔드에서 실행될 궛으로 추론함
        // "lib": ["ES6", "DOM"],
        "lib": ["ES6"],

        //
        "strict": true,

        //
        // "allowJs": true,        

        "module": "CommonJS"
    }
}